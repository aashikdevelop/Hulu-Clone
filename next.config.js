/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["image.tndb.org"],
    domains: ["links.papareact.com", "image.tmdb.org"],
  },
};

module.exports = nextConfig;
